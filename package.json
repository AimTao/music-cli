{
  "name": "music-cli",
  "version": "1.0.0",
  "description": "Agent-ready music CLI — JSON output, batch pipe, zero config",
  "main": "./dist/index.js",
  "bin": {
    "music-cli": "./bin/music-cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "prepublishOnly": "npm run build",
    "prepare": "npm run build"
  },
  "keywords": [
    "music",
    "download",
    "cli",
    "youtube",
    "bilibili",
    "lyrics"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "commander": "^12.0.0",
    "node-id3": "^0.2.9"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "files": [
    "dist",
    "bin",
    "README.md"
  ],
  "engines": {
    "node": ">=16.0.0"
  }
}
